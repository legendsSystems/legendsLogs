Notify = {}

Notify.Success = function(target, text)
	--exports.legendsSystems_Notify:Notify({ title = "Success!", message = text, color = "#93CAED", target = target, timeout = 15 })
end

Notify.Error = function(target, text)
	--exports.legendsSystems_Notify:Notify({ title = "Error!", message = text, color = "#93CAED", target = target, timeout = 15 })
end

Notify.Custom = function(target, title, text)
	--exports.legendsSystems_Notify:Notify({ title = title, message = text, color = "#93CAED", target = target, timeout = 15 })
end
